services:
  - type: web
    name: jarves-command-center
    env: node
    plan: free
    buildCommand: npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.10.0
      - key: NPM_FLAGS
        value: --include=dev
